import fs from 'node:fs/promises';
import path from 'node:path';
export async function detectProject(root){ const files=await fs.readdir(root,{withFileTypes:true}); const has=(n)=>files.some(f=>f.name===n); if(has('index.html')) return {kind:'static',label:'static website',directory:root}; if(has('package.json')){ let pkg; try{pkg=JSON.parse(await fs.readFile(path.join(root,'package.json'),'utf8'));}catch{throw new Error('Invalid package.json');} if(has('vite.config.js')||has('vite.config.ts')||pkg.dependencies?.vite) return {kind:'vite',label:'Vite project',directory:root}; if(pkg.dependencies?.next||has('next.config.js')||has('next.config.mjs')||has('next.config.ts')) return {kind:'next',label:'Next.js project',directory:root}; return {kind:'node',label:'Node.js project',directory:root}; } throw new Error('Trama could not determine how to deploy this project.'); }
